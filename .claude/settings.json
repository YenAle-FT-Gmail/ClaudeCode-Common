{
    "model": "claude-3-5-sonnet-20240620",
    "permissions": {
        "allowed_tools": [
            "git diff --staged"
            // Add common, safe commands for THIS project here.
            // Example for Python: "poetry run pytest"
            // Example for Node: "npm test"
        ]
    },
    "hooks": {
        "PostToolUse": [
            {
                "tool_name": "edit_file",
                // This command assumes Prettier/ESLint is configured.
                // Customize or remove it if your project uses different tools.
                "command": "npm run format {{filename}}",
                "show_output": false
            }
        ]
    }
}